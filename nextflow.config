/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    galantelab/precisecaller Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Default config options for all compute environments
----------------------------------------------------------------------------------------
*/

// Global default params, used in configs
params {
    /*
     * Input options
     * --------------
     * Primary input definition for the pipeline, typically a samplesheet
     * describing samples, lanes and FASTQ files.
     */
    input                      = null

    /*
     * Reference genome configuration
     * -------------------------------
     * Controls which reference genome is used and how reference files
     * are retrieved and managed.
     */
    genome                     = 'GATK.GRCh38'                   // Genome key (e.g. hg38, grch37)
    igenomes_base              = 's3://ngi-igenomes/igenomes/'   // Base path for iGenomes references
    igenomes_ignore            = false                           // Disable iGenomes lookup entirely
    save_reference             = false                           // Publish reference files to output

    wgs        = false     // Set to true, if data is a whole genome sequencing file
    intervals  = null      // BED file with target regions. It is not mandatory for WGS

    /*
     * FASTQ splitting
     * ----------------
     * Controls optional splitting of large FASTQ files into smaller chunks
     * to increase parallelism in downstream steps.
     */
    split_fastq_reads     = 50_000_000  // Set to 0 in order to disable splitting
    save_split_fastq      = false       // Publish split FASTQ files

    /*
     * UMI-tagged reads processing
     * ----------------------------
     * Parameters controlling UMI-aware preprocessing and consensus calling.
     * When UMI processing is enabled, trimming and standard alignment are skipped.
     */
    umi_read_structure       = null        // Read structure string; null disables UMI processing
    group_by_umi_strategy    = 'Adjacency' // Default strategy when running with UMI for GROUPREADSBYUMI
    umi_file                 = null        // No fixed list of UMIs is in use for CORRECTUMIS
    umi_file_max_mismatches  = 1           // Maximum number of mismatches between a UMI and an expected UMI
    umi_file_min_distance    = 2           // Minimum distance (in mismatches) to next best UMI
    umi_min_reads            = 3           // The minimum number of reads supporting a consensus base/read
    umi_min_base_quality     = 45          // Mask (make 'N') consensus bases with quality less than this threshold
    umi_max_base_error_rate  = 0.2         // The maximum error rate for a single consensus base

    /*
     * Read trimming and filtering
     * ----------------------------
     * Parameters passed to FastP for adapter trimming, quality trimming,
     * and read filtering. Skipped automatically when UMI processing is enabled.
     */
    skip_trimming         = true        // Skip trimming at all
    skip_adapter_trimming = false       // Skip adapter trimming
    trim_front1           = 0           // Remove bases from the 5' end of read 1
    trim_front2           = 0           // Remove bases from the 5' end of read 2
    trim_tail1            = 0           // Remove bases from the 3' end of read 1 AFTER adapter/quality trimming has been performed
    trim_tail2            = 0           // Remove bases from the 3' end of read 2 AFTER adapter/quality trimming has been performed
    trim_poly_g           = false       // Remove poly-G tails
    trim_min_read_length  = 15          // Reads shorter will be discarded
    save_trimmed_fastq    = false       // Publish trimmed FASTQ files

    min_mapq = 20 // Mapping filtering by phred-score. Set to 0 in order to disable mapq filtering

    /*
     * Collect metrics
     * ---------------
     * Parameter controlling the collection of metrics.
     * If not provided, assay-specific defaults will be used (e.g. WGS vs WXS/panel).
     */
    skip_alignment_metrics     = null   // Skip alignment metrics
    skip_coverage_metrics      = null   // Skip coverage metrics
    skip_recalibration_metrics = null   // Skip recalibration metrics
    coverage_metrics_mins      = null   // Minimum coverage thresholds (comma-separated) used to compute coverage statistics
    coverage_metrics_intervals = null   // Coverage depth intervals (colon-separated, 1:10:20) used to summarize depth distribution

    /*
     * Read group and sequencing metadata
     * -----------------------------------
     * Metadata written to BAM read group (RG) fields during alignment
     * or UMI-aware processing.
     */
    seq_center    = null       // No sequencing center to be written in read group CN field by aligner
    seq_platform  = 'ILLUMINA' // Default platform written in read group PL field by aligner

    /*
     * MultiQC reporting
     * ------------------
     * Customization options for the final MultiQC report.
     */
    multiqc_config             = null        // Custom MultiQC configuration file
    multiqc_title              = null        // Custom title for the report
    multiqc_logo               = null        // Custom logo
    max_multiqc_email_size     = '25.MB'     // Maximum size for email attachments
    multiqc_methods_description = null       // Custom methods description text

    /*
     * Output, notifications and logging
     * ----------------------------------
     * Controls output directory handling, file publishing behavior,
     * and email notifications.
     */
    outdir                       = null      // Base output directory
    publish_dir_mode             = 'copy'    // How files are published (copy, symlink, move)
    email                        = null      // Email address for pipeline notifications
    email_on_fail                = null      // Email address for failure notifications
    plaintext_email              = false     // Send emails in plain text
    monochrome_logs              = false     // Disable ANSI colors in logs

    /*
     * Command-line helper flags
     * --------------------------
     * Flags controlling help, version output and parameter visibility.
     */
    help                         = false
    help_full                    = false
    show_hidden                  = false
    version                      = false

    /*
     * Test dataset locations
     * -----------------------
     * Paths used by nf-test and pipeline tests to retrieve example data.
     */
    pipelines_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/'
    modules_testdata_base_path   = 'https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/'
    local_testdata_base_path     = 'https://bioinfohsl-tools.s3.amazonaws.com/precisecaller/test-datasets/'

    /*
     * Configuration profile metadata
     * --------------------------------
     * Metadata describing the active configuration profile.
     */
    config_profile_name        = null
    config_profile_description = null
    config_profile_contact     = null
    config_profile_url         = null

    /*
     * Custom configuration loading
     * ------------------------------
     * Controls loading of external nf-core configuration profiles.
     */
    custom_config_version      = 'master'
    custom_config_base         = "https://raw.githubusercontent.com/nf-core/configs/${params.custom_config_version}"

    /*
     * Parameter validation
     * ---------------------
     * Enable or disable schema-based parameter validation.
     */
    validate_params            = true
}

// Load base.config by default for all pipelines
includeConfig 'conf/base.config'

profiles {
    debug {
        dumpHashes              = true
        process.beforeScript    = 'echo $HOSTNAME'
        cleanup                 = false
        nextflow.enable.configProcessNamesValidation = true
    }
    conda {
        conda.enabled           = true
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        conda.channels          = ['conda-forge', 'bioconda']
        apptainer.enabled       = false
    }
    mamba {
        conda.enabled           = true
        conda.useMamba          = true
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    docker {
        docker.enabled          = true
        conda.enabled           = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
        docker.runOptions       = '-u $(id -u):$(id -g)'
    }
    arm {
        docker.runOptions       = '-u $(id -u):$(id -g) --platform=linux/amd64'
    }
    singularity {
        singularity.enabled     = true
        singularity.autoMounts  = true
        conda.enabled           = false
        docker.enabled          = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    podman {
        podman.enabled          = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    shifter {
        shifter.enabled         = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    charliecloud {
        charliecloud.enabled    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        apptainer.enabled       = false
    }
    apptainer {
        apptainer.enabled       = true
        apptainer.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
    }
    wave {
        apptainer.ociAutoPull   = true
        singularity.ociAutoPull = true
        wave.enabled            = true
        wave.freeze             = true
        wave.strategy           = 'conda,container'
    }

    test      { includeConfig 'conf/test.config'      }
    test_full { includeConfig 'conf/test_full.config' }
}

// Load nf-core custom profiles from different Institutions
includeConfig !System.getenv('NXF_OFFLINE') && params.custom_config_base ? "${params.custom_config_base}/nfcore_custom.config" : "/dev/null"

// Load galantelab/precisecaller custom profiles from different institutions.
// TODO nf-core: Optionally, you can add a pipeline-specific nf-core config at https://github.com/nf-core/configs
// includeConfig !System.getenv('NXF_OFFLINE') && params.custom_config_base ? "${params.custom_config_base}/pipeline/precisecaller.config" : "/dev/null"

// Set default registry for Apptainer, Docker, Podman, Charliecloud and Singularity independent of -profile
// Will not be used unless Apptainer / Docker / Podman / Charliecloud / Singularity are enabled
// Set to your registry if you have a mirror of containers
apptainer.registry    = 'quay.io'
docker.registry       = 'quay.io'
podman.registry       = 'quay.io'
singularity.registry  = 'quay.io'
charliecloud.registry = 'quay.io'

// Load igenomes.config if required
includeConfig !params.igenomes_ignore ? 'conf/igenomes.config' : 'conf/igenomes_ignored.config'

// Load assays.config
includeConfig 'conf/assays.config'

// Export these variables to prevent local Python/R libraries from conflicting with those in the container
// The JULIA depot path has been adjusted to a fixed path `/usr/local/share/julia` that needs to be used for packages in the container.
// See https://apeltzer.github.io/post/03-julia-lang-nextflow/ for details on that. Once we have a common agreement on where to keep Julia packages, this is adjustable.

env {
    PYTHONNOUSERSITE = 1
    R_PROFILE_USER   = "/.Rprofile"
    R_ENVIRON_USER   = "/.Renviron"
    JULIA_DEPOT_PATH = "/usr/local/share/julia"
}

// Set bash options
process.shell = """\
bash

set -e # Exit if a tool returns a non-zero status/exit code
set -u # Treat unset variables and parameters as an error
set -o pipefail # Returns the status of the last command to exit with a non-zero status or zero if all successfully execute
set -C # No clobber - prevent output redirection from overwriting files.
"""

// Disable process selector warnings by default. Use debug profile to enable warnings.
nextflow.enable.configProcessNamesValidation = false

def trace_timestamp = new java.util.Date().format( 'yyyy-MM-dd_HH-mm-ss')
timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_timeline_${trace_timestamp}.html"
}
report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_report_${trace_timestamp}.html"
}
trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_trace_${trace_timestamp}.txt"
}
dag {
    enabled = true
    file    = "${params.outdir}/pipeline_info/pipeline_dag_${trace_timestamp}.html"
}

manifest {
    name            = 'galantelab/precisecaller'
    author          = """Thiago L. A. Miller / Pedro A. F. Galante"""
    homePage        = 'https://github.com/galantelab/precisecaller'
    description     = """A complete, scalable, easy-to-use, open-source platform for genomic variant detection in Oncology and Precision Medicine"""
    mainScript      = 'main.nf'
    nextflowVersion = '!>=24.04.2'
    version         = '1.3.0dev'
    doi             = ''
}

// Nextflow plugins
plugins {
    id 'nf-schema@2.1.1' // Validation of pipeline parameters and creation of an input channel from a sample sheet
}

validation {
    defaultIgnoreParams = ["genomes"]
    help {
        enabled = true
        command = "nextflow run $manifest.name -profile <docker/singularity/.../institute> --input samplesheet.csv --outdir <OUTDIR>"
        fullParameter = "help_full"
        showHiddenParameter = "show_hidden"
    }
}

// Load modules.config for DSL2 module specific options
includeConfig 'conf/modules/alignment.config'
includeConfig 'conf/modules/alignment_metrics.config'
includeConfig 'conf/modules/bqsr.config'
includeConfig 'conf/modules/coverage_metrics.config'
includeConfig 'conf/modules/filter_mapq.config'
includeConfig 'conf/modules/haplotypecaller.config'
includeConfig 'conf/modules/merge.config'
includeConfig 'conf/modules/prepare_genome.config'
includeConfig 'conf/modules/prepare_intervals.config'
includeConfig 'conf/modules/qc.config'
includeConfig 'conf/modules/sort.config'
includeConfig 'conf/modules/split.config'
includeConfig 'conf/modules/trim.config'
includeConfig 'conf/modules/umi.config'
